{ parameter unit ;
  storage (big_map nat (pair (nat %token_id) (map %token_info string bytes))) ;
  code { CDR ; NIL operation ; PAIR } ;
  view "token_metadata"
       nat
       (pair (nat %token_id) (map %token_info string bytes))
       { UNPAIR ;
         GET ;
         IF_NONE { PUSH string "FA2_TOKEN_UNDEFINED" ; FAILWITH } {} } }

